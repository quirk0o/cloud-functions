const exec = require('child_process').exec
const execSync = require('child_process').execSync
const hello = require('@quirk0.o/exec-addon').hello

module.exports = (context, callback) => {
  callback(null, {})
}
